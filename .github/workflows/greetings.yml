name: Greetings

on: 
  [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write

    steps:
      - uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: >
            👋 Hey there! Thanks for opening your first issue on this repo!  
            We really appreciate your contribution — someone from the team will check it out soon. 🚀
          pr-message: >
            🎉 Thanks for submitting your first pull request!  
            You’re officially part of the project’s contributor list — we’ll review it ASAP. 💪
